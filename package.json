{
  "name": "peerchat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir client-bin && mkdir server-bin && pkg index.js --out-path client-bin/ && pkg server.js --out-path server-bin"
  },
  "author": "",
  "license": "GPL-3.0"
}
